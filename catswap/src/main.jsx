import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import CatSwap from "./CatSwap";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CatSwap />
  </React.StrictMode>
);
